<template>
  <section class="placeholder-view">
    <h2>Ativos</h2>
    <p>Esta area vai receber o CRUD dos ativos e a criacao automatica do estado mensal inicial.</p>
  </section>
</template>

<style scoped>
.placeholder-view {
  display: grid;
  gap: 10px;
  padding: 22px;
  border: 1px solid var(--glass-border);
  border-radius: 24px;
  background: var(--glass-surface);
  box-shadow: var(--shadow);
  backdrop-filter: blur(22px);
}

.placeholder-view p {
  color: var(--text);
}
</style>
