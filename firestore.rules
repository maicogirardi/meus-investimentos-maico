rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Initial safe default for a private personal app.
    // We can tighten these rules around user-scoped documents as the data model settles.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

